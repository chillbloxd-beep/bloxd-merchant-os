{
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:migrate:local": "wrangler d1 migrations apply bloxd_merchant_os --local",
    "d1:migrate:remote": "wrangler d1 migrations apply bloxd_merchant_os --remote"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
